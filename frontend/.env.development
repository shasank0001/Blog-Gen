# Frontend Development Environment Variables
# This file is used when running frontend locally (npm run dev)

# For local development, use localhost
VITE_API_URL=http://localhost:8000

# Note: When running with Docker Compose, the docker-compose.yml
# will override this to use http://backend:8000
